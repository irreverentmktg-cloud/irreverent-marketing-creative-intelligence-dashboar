# Required for Claude calibration + scoring
ANTHROPIC_API_KEY=

# Supabase (project URL + service-role key — server-side only, never ship to web)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional — wire these later. Stubs are returned until the keys are present.
OPENAI_API_KEY=
PERPLEXITY_API_KEY=
GOOGLE_API_KEY=

# Where the web app is served from (used for CORS in dev)
WEB_ORIGIN=http://localhost:5173
PORT=8787
